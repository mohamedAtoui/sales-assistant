# Mistral API key — https://console.mistral.ai
MISTRAL_API_KEY=

# Shared password the sales team types at /login
AUTH_PASSWORD=

# Session signing secret — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Base URL of the deployment (http://localhost:3000 in development)
NEXTAUTH_URL=http://localhost:3000
